services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "${BIND_HOST:-0.0.0.0}:${PORT:-3000}:${PORT:-3000}"
    environment:
      NODE_ENV: production
      PORT: ${PORT:-3000}
    volumes:
      - app-data:/app/data

volumes:
  app-data:
